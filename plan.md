# ClearskyGoto 开发计划

ClearskyGoto 是针对晴空谐波赤道仪系列开发的 Android 控制 App。本项目在 MountBehave 代码基础上继续开发（当前版本 `0.0.1`），后续规划以晴空设备的实机流程、界面一致性和现场可诊断性为核心。

## 目标

1. 为晴空谐波赤道仪提供简洁、可靠的 Android 控制界面。
2. 把常用操作集中到星图：选目标、GOTO、同步、手控、校准。
3. 保持真实设备安全优先：停止始终可见，Park 状态禁用危险操作，写入固件配置后强制重连。
4. 提供足够诊断信息：命令日志、状态快照、错误上下文和可导出日志。
5. 保持离线可用：恒星、深空、星座线、银河背景和主要太阳系天体星历均随 APK 打包。

## 当前已完成

- App 命名与界面主体切换为 ClearskyGoto。
- 设置页重构为晴空控制台风格。
- 连接/同步页压缩连接、站点、时间和跟踪控制。
- 手控与星图合并为“星图”页。
- 半透明四向控制盘支持显示/隐藏和拖动。
- 星图内可展开支架校准面板，收起面板不取消校准流程。
- 两星/三星校准与三星后极轴精调逻辑保留。
- OnStepX 支架类型切换采用无回复发送、读回校验和强制断开。
- Park 状态下禁用 GOTO、同步、跟踪和方向控制。
- 中英文 UI 切换并持久化。
- JPL DE440s 主要太阳系天体抽样表覆盖 2025-2050。
- 小天体按名称添加和候选过滤已实现。
- 命令日志系统覆盖 TX/RX、用户动作、状态快照、异常和导出。

## 近期计划

### 1. 晴空实机回归

- 在晴空 ST17 上回归连接、断开、停止、方向移动和移动速率。
- 回归星图 GOTO、取消 GOTO、到位复核和同步。
- 回归两星/三星校准，确认收起校准面板后流程仍保持。
- 回归经纬仪模式下双轴跟踪和隐藏极轴精调。
- 导出日志并确认现场问题可追踪。

### 2. UI 收敛

- 检查窄屏、宽屏、横屏和系统导航栏遮挡。
- 继续压缩星图页垂直占用，让主星图区域优先。
- 统一所有小图标、问号按钮、下拉文字和状态文本尺寸。
- 确认夜视模式下侧边栏、悬浮按钮和卡片颜色一致。

### 3. 星历与数据

- 保持主要太阳系天体 2025-2050 星历资产随 APK 发布。
- 明确超出覆盖范围时的回退提示。
- 小天体继续使用 JPL SBDB 按需添加。
- 根据实测反馈决定是否增加小天体过期提示和删除入口。

### 4. 日志与反馈

- 确保所有新 UI 入口都有用户动作日志。
- 保持导出前 flush，避免刚出错时日志缺尾。
- 继续优化日志隐私提示和导出路径。
- 为实机测试建立固定反馈模板。

## 相机识别与极轴校准（开发中）

相机盲解已落地（phase 2：前景感知检测 + 三角形盲解 + 叠加，见 `PlateSolver`/`StarDetector.detectForSolve`/`SolveOverlayView`）。下一阶段拆成两层，先做图像模块，极轴校准复用它。

### 层一：独立图像识别/标注模块

只负责"给一张图，识别星空并标注"，不碰赤道仪。统一入口 `ImageSolveEngine`（持有单例 `SkyCatalog`+`PlateSolver`，构建一次复用），输入 `Bitmap + SolveInput`（焦距/FOV 先验来源），输出检测 + 解算（中心 RA/Dec、FOV、旋转、匹配星、RMS）+ 叠加 + 诊断。相机与相册共用同一套算法。

要点与坑：

- 检测/解算核心本就与相机解耦；重构主要是把 `CameraPanel` 的**编排**抽出来，**必须保住现有并发守卫**（`panelGeneration`/`solveGeneration`/`solveInFlight`，已修过 stale-solve 竞态）。
- `ImageSolveEngine` 持**单例** `PlateSolver`（三角索引约 16.4 万条、构建数百 ms），相册多图不得重建。
- 相册 FOV 先验不可靠是最大风险（解算仅在焦距先验 ±20% 内接受）。因三角 seeding 尺度无关，焦距只用于 scale-gate/verify，故先验未知时 engine 应**自动多 FOV 搜索**（如 25/45/65/90° 或放宽 gate）取能解者，"档位选择/手动输入/失败重试"作兜底。
- 相册另两坑：**超广角畸变**破坏针孔模型（主摄 JPEG 已校正、验证 OK；超广角最难，建议标注或暂不支持）；**偏心裁剪**破坏主点居中假设（裁剪须居中）。
- 导入须正确处理 EXIF/MediaStore 方向旋转；能读 35mm 等效焦距则估 FOV。

### 层二：相机极轴校准（赤道仪工作流，需连 OnStep）

前提：手机须**刚性固定**在随 RA 轴转动的结构上；仅**赤道仪模式**适用；相机 boresight 不能与 RA 轴重合（需有夹角）。

原理（NINA/SharpCap 式多点盲解极轴法）：绕 RA 轴转动并多次盲解，反推 RA 轴在天球的方向，与天极比较得偏差。**因盲解给完整三轴姿态 `R`（约定 `R` 为 celestial→camera，见 `PlateSolver.Solution.r`），两次解算的相对旋转 `ΔR = R_b​ᵀ·R_a`（代码 `PolarAlignment` 即按此实现）其旋转轴即 RA 轴方向——2 次足够，第 3 次降噪**（测量时关跟踪或按时间戳补偿恒星运动）。注意方向约定：写反成 `R_a·R_bᵀ` 会得到反向轴。

难点（须先想清）：把天球极轴偏差**分解为方位/高度两个螺丝的可执行方向并实时更新**（拧螺丝时 boresight 随之移动，可持续拍摄解算刷新箭头）；测量阶段用 RA 电机，校正阶段只拧螺丝、不用手控。

**精度预期（先拍板）**：手机超广角约 64″/px → 轴向定位约**几角分**，比成像相机（角秒级）粗 30–60 倍，只适合"粗调/辅助"级，长曝光摄影不够。

### 实施顺序

1. **模块化图像识别**：抽 `ImageSolveEngine`/`SolveInput`，实时拍摄走 engine，行为不变。（已完成；engine 为应用级共享单例，`load()` 单飞构建）
2. **相册导入**：相机页"拍摄/从相册选择"，EXIF 方向、35mm 焦距估 FOV、未知 FOV 自动网格搜索、手动 FOV 选择/重试。引入 `ImageSolveResult`/`ImageSource`，`StarDetectionView`→`SolveOverlayView`。（已完成）
3. **极轴校准（手动版）**：实现为相机页内的"极轴校准"模式（非独立页面）。手机刚性固定后，绕 RA 轴转动并多次拍摄/导入，累计解算，由 `ΔR=R₂ᵀ·R₁` 反推 RA 轴并给出极轴偏差与高度/方位分解。（已完成；几何离线验证：已知轴恢复误差 0）
4. **自动 RA 旋转**：手动版确认后接 OnStep 自动转轴/停止/拍照/解算。

## 发布前检查

每次发布前至少完成：

1. `.\scripts\build-debug.ps1`
2. `.\scripts\build-release.ps1`
3. 电脑端预览启动。
4. README、CHANGELOG、测试指南和数据说明同步更新。
5. 检查 APK 内是否包含 `solar_major_vectors.bin`。
6. 真机完成连接、停止、GOTO、同步、校准和日志导出基本回归。
7. Git 状态确认无遗漏新增资源。

## 长期方向

- 根据更多晴空型号实机反馈调整默认连接参数、跟踪语义和支架模式文案。
- 为常见现场问题提供更直接的 UI 提示。
- 评估是否把较大的星历二进制资产迁移到 Git LFS 或发布流程注入。
- 持续降低普通用户看到的调试感，让界面更像天文设备控制台，而不是工程面板。
