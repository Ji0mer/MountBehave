# Changelog

本文记录 ClearskyGoto 的主要变更。ClearskyGoto 是针对晴空谐波赤道仪开发的 Android 控制 App。版本号从 `0.0.1` 重新计数；上游 MountBehave 的历史保留在文末。

## v0.0.1 — 2026-05-06

ClearskyGoto 首个对外发布版本。在 MountBehave 代码基础上完成针对晴空谐波赤道仪的整体改造、稳定性修复和性能优化。

### 新增

- App 命名、图标、文案、文档统一为 ClearskyGoto。
- 设置页、连接/同步页、星图页按晴空谐波赤道仪现场操作重新布局；连接面板压缩 IP、端口、跟踪速率和启动跟踪控制。
- 手控和星图合并为同一个"星图"页面，方向控制盘改为半透明四向虚拟摇杆，可显示/隐藏、可拖动位置。
- 支架校准改为星图内可展开面板，收起面板不取消正在运行的校准流程。
- 校准按钮 "居中后接受" 统一为 "居中后同步"。
- 中文/英文 UI 切换，选择保存到下次启动；切换时如有 SBDB 抓取等后台 IO 会被阻断。
- 数据来源声明对话框（HYG / OpenNGC / 星座连线 JSON / DE440s+Skyfield / JPL SBDB）。
- 太阳/月亮/主要行星位置使用 JPL DE440s 抽样表，覆盖 2025-01-01 至 2050-01-01，资源约 7.5 MiB；超出范围或资产损坏时回退解析模型并记录一次性警告。
- 资产校验：装载时验证 magic、payload size 和首样本太阳距离，端序错误或损坏会被拦下。
- 资产后台线程加载，避免 UI 启动时阻塞。
- 月球（含解析回退）和太阳/行星位置应用观测者地心视差矫正；行星视星等基于地心/日心距离与相位角粗略变化。
- 太阳/月亮/行星标签随中英文 UI 切换。
- 紧急停止现在同时发送 `:Q#` 和 `:Td#`，确保运动停止与跟踪关闭。
- 紧停后下一次 GOTO 会等到达目标后再恢复跟踪（基于 `:MS#` 接受 + 状态轮询 "arrived"），单轴 / 双轴跟踪根据校准模型自动选用。
- 观测时间手动改动后再点 "同步时间/地点" 会先确认是否真的写入手动时间到赤道仪 RTC。
- 观测地/时间未同步到赤道仪时，相关字段以红色感叹号高亮提示。
- 星图目标过中天时间叠加在卡片上。
- 切换页面时主滚动视图自动滚回顶部。

### 修复 / 稳定性

- **修复手机上星图整片蓝屏问题**：`createSkyChartHeader()` 中作为权重 spacer 的空 `View` 在某些 Android LinearLayout 实现下被反向撑成几千像素，把 chartFrame 推到屏幕外。改为显式 `dp(1)` 高度。同时审计所有 `weightWrap(1f)` 用法，加 Javadoc 警告防止再次回归。
- **修复 Milky Way 在硬件 Canvas 上的兼容性问题**：`drawBitmapMesh + colors[] + PorterDuffXfermode(SCREEN)` 在多种 GPU 驱动上会让整帧 RenderNode 静默失效。现在 mesh 只在软件位图上执行；硬件 Canvas 只调用 `drawBitmap` + `prepareToDraw()`。`SkyChartView` 同时分配独立的 `LAYER_TYPE_HARDWARE` 离屏纹理，隔离父 FrameLayout 的合成问题。
- 紧急停止 / GOTO / 跟踪状态机覆盖到所有路径（连接、断开、停放、解除、紧停、手控、校准保存、Refine PA 应用、连接丢失、GOTO 拒绝/超时/到位/手动取消等），不会留下"自动恢复跟踪"的悬挂状态。
- `toggleTracking` 显式取消 pending 的 GOTO-后跟踪恢复，防止用户主动开关跟踪后被自动覆盖。
- `Bitmap` / `Canvas` 异常时优雅降级显示一行错误文案而不是闪退。
- 切语言 / 屏幕旋转时 `MajorSolarSystemEphemeris` 静态加载状态保留，避免重复 IO。
- 主页面切换时强制三个 page 都进入正确 visibility，避免遗留布局空间。
- 默认 GPS 申请限制在权限已授予的情况下静默调用，不强行弹权限对话框。
- 日期字段解析放宽：支持 `yyyy-MM-dd` / `yyyy/MM/dd` / `yyyy.MM.dd` / `yyyy-M-d` / `M-d` 等多种格式。
- 命令日志开关持久化到 SharedPreferences，重启后保留用户选择。

### 性能

- **Milky Way 帧间缓存**：当相机方向（< 0.5°）和恒星时（< 0.5° ≈ 真实时间 2 分钟）变化都很小时，跳过 4608 顶点的 mesh 重计算，复用已渲染好的位图。静态浏览星图时单帧从 ~150 ms 降到 ~5 ms。
- 太阳系天体位置在每帧 `onDraw` 内只计算一次，绘制、彗尾找太阳、目标查找、命中检测共用同一份。
- `.bin` 资产用 `noCompress`，APK 内零拷贝。

### 项目

- Android 版本号：`versionName 0.0.1` / `versionCode 1`。
- 最低支持：Android 8.0 (API 26)；目标：API 36。
- 实机验证机型：晴空 ST17。

## 上游 MountBehave 历史

ClearskyGoto 之前的代码作为 MountBehave 经历了 `v0.1.0` ~ `v0.2.5` 的迭代，主要里程碑包括：

- `v0.2.5` —— 设置页状态优先布局、夜视模式视觉统一。
- `v0.2.4` —— 星图同步、多点指向修正、`E8` 错误修复。
- `v0.2.3` —— 北天极 GOTO、限位识别、银河背景、星图时间手动设置。
- `v0.2.2` —— 移除 Set/Return Home、连续 GOTO 状态判断改进。
- `v0.2.1` —— 星图/校准 UI 压缩、桌面预览拖拽修复。
- `v0.2.0` —— 设置页 / 连接同步页拆分、OnStepX 适配、命令日志系统、小天体支持。
- `v0.1.0` —— 初始可用：WiFi 连接、方向控制、星图、GOTO、观测地同步、跟踪、两星/三星校准、日志诊断。
