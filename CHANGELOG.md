# Changelog

本文记录 ClearskyGoto 的主要变更。ClearskyGoto 是针对晴空谐波赤道仪开发的 Android 控制 App，代码基础来自 MountBehave `v0.2.5`。

## 当前开发版

- App 命名、图标、README、测试文档和数据文档统一为 ClearskyGoto。
- 设置页、连接/同步页和星图页按晴空谐波赤道仪现场操作重新布局。
- 手控和星图合并为“星图”页面，方向控制盘改为半透明四向控制盘。
- 支架校准改为星图内可展开面板，收起面板不取消正在运行的校准流程。
- 新增中文/英文 UI 切换，并保存为后续默认语言。
- 主要太阳系天体位置改为 JPL DE440s 抽样表，覆盖 2025-01-01 至 2050-01-01；超出范围回退解析模型并记录警告。
- 星历资产后台加载、资源完整性校验、行星亮度近似变化和英文标签切换已完成。

## v0.2.5

- 设置页改为状态优先的控制台布局：连接、安全、GOTO、停放、夜视和日志信息更集中。
- 连接卡片压缩 IP、端口、跟踪速率和启动跟踪控制。
- 星图银河背景可见度调整为更适合目视参考的低亮度叠加。
- 夜视模式、停止、GOTO、停放等控件统一为更克制的暗色视觉风格。
- Android 版本号：`versionName 0.2.5` / `versionCode 7`。

## v0.2.4

- 新增星图同步：GOTO 到位后手动居中，再发送 OnStep 同步命令。
- 保存两星/三星模型后，星图同步会维护本地多点指向修正。
- GOTO 与取消合并为同一个状态按钮。
- 修复同步太快导致 OnStep 返回 `E8` 的问题。
- 改进手控停止和 WiFi 抖动处理。
- 校准残差超过 `1°` 时只提示风险，不强制中断。

## v0.2.3

- 修复北天极附近 GOTO 到位后状态卡住的问题。
- 增加 GOTO 静止检测、限位错误识别和“需手控恢复”保护。
- 日志导出支持保存到下载目录。
- 星图加入 NASA SVS 银河背景。
- 星图时间可手动设置，并在会移动赤道仪的操作前自动恢复到当前时间。
- 校准 UI 压缩，最后一颗星接受后自动保存模型。
- 横竖屏旋转时尽量保持连接和运行状态。

## v0.2.2

- 移除 Set Home / Return Home 入口，避免把 OnStep 机械参考点误用为普通目标。
- 改进连续 GOTO 状态判断，防止赤道仪实际未到位但 App 提前释放。
- Park、停止、断开和手控动作会清理本地 GOTO 状态。

## v0.2.1

- 压缩星图和校准区域 UI。
- 修复桌面预览/触控板场景下星图拖拽被误判为缩放的问题。
- 菜单展开时显示当前版本号。
- 可见文本统一使用“支架”。

## v0.2.0

- 新增独立设置页、连接/同步页和 OnStepX 适配。
- OnStepX 支架类型切换改为无回复命令发送、读回校验和强制断开。
- 新增命令日志系统，覆盖 TX/RX、用户动作、状态快照和异常。
- 改进小天体下载、彗星逐颗添加和太阳系/小天体显示。
- 两星模型仅用于 GOTO / 跟踪补偿，三星或更多星模型后才解锁极轴精调。

## v0.1.0

- 初始可用版本：WiFi 连接、方向控制、星图、GOTO、观测地/时间同步、跟踪、两星/三星校准和日志诊断。
- 已在晴空 ST17 上完成基础连接、手控、停止、两星校准和 GOTO 流程测试。
